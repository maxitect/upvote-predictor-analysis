# To create the conda environment, run:
#   conda env create -f environment.yml
name: upvote-predictor
channels:
  - defaults
  - conda-forge
dependencies:
  - python=3.13.3
  - psycopg=3.2.6
  - pandas=2.2.3
  - matplotlib=3.10.0
  - seaborn=0.13.2
